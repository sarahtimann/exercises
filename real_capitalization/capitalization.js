"use strict";

let name = "Sarah";
let newName = "susanne";

// Lav om til små bogstaver
console.log(newName.toLowerCase());

// Lav om til store bogstaver
console.log(newName.toUpperCase());

console.log(newName[0].toUpperCase() + newName.substring(1).toLowerCase());
